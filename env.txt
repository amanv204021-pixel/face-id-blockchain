# ═══════════════════════════════════════════════════════════════
#  FACE ID + BLOCKCHAIN VERIFICATION — .env
#  Works as-is in DEMO MODE (no keys needed). Fill in a section
#  only when you want LIVE search / real testnet anchoring.
#  Used by: backend (FastAPI). Frontend needs nothing (vite proxies /api).
# ═══════════════════════════════════════════════════════════════

# ── REVERSE IMAGE SEARCH ──────────────────────────────────────
# demo          = local fixture index (synthetic images, clearly labeled) ← default
# google_vision = Google Cloud Vision WEB_DETECTION (official API)
REVERSE_SEARCH_PROVIDER=demo
GOOGLE_VISION_API_KEY=

# ── BLOCKCHAIN (optional) ─────────────────────────────────────
# Leave all three empty → local simulated chain (zero setup, clearly labeled).
# Fill ALL THREE + `pip install web3` → anchors via FaceRecordAnchor.sol.
# ⚠ TESTNET ONLY. Never put a real mainnet key in this file.
BLOCKCHAIN_RPC_URL=
BLOCKCHAIN_PRIVATE_KEY=
CONTRACT_ADDRESS=

# Example (Sepolia via Alchemy/Infura):
# BLOCKCHAIN_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
# BLOCKCHAIN_PRIVATE_KEY=0xabc123...   (test wallet!)
# CONTRACT_ADDRESS=0x...               (from blockchain/scripts/deploy.js)
